# Get detail of docker
# Inspect image
docker image inspect image-name
# Inspect container
docker container inspect container-name
# Inspect volume
docker volume inspect volume-name
# Inspect network
docker network inspect network-name
